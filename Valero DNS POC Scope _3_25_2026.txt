| # | Workstream | One-Line Description |


Script flows: 

Resource group
VNET
Create Public DNS zone
Bind import into Azure DNS public Zone
Traffic manager profile creation and its dependencies
WebApp (UK and US east)each region and associate with DNS zone, https
Failover
Automation of certicate validation



FunctionApps for DNS testing in multiple geo locations-to do nslookup in multi-region-emulate the client side with this to run ndlookup against DNS server
customer to provide a public endpoints to test




|---|---|---|

| 1 | Bind Parity | Authoritative lookups — all record types (A, AAAA, CNAME, MX, TXT, SRV, NS, SOA) |

| 2 | Zone Import | Import 2–3 Bind zone files into Azure DNS, verify record counts |

| 3 | RBAC & Delegation | Operator role (records only) vs Admin role (full zone control) |

| 4 | Automated Record Creation | Scripted DNS record CRUD via CLI/API/BICEPS |

| 5 | DCV TXT Automation (Certs) | Automated _acme-challenge TXT record creation for certificate domain validation — single domain, subdomain, wildcard, and multi-domain SAN certs — with proven resolution and cleanup |

DigiCert at current state


| 6 | DNS Logging → SIEM | Query logs flow to Valero's SIEM via Event Hub |- Qradar



| 7 | Propagation & Latency | Update latency and propagation timing acceptable | -TBD


Required:
Zone Migration
Audit Logging
Reporting
Zone Snapshots
Certificate Integration-DCV
 
Optional:  Nice to have

DNS Record Failover-traffic manager
Weighted Load Balancing-traffic manager
Geographic DNS-traffic manager

API Support
DNSSEC-not a requirement


Jeremy, Mike, Matt-Key stakeholders

Date & Time :  Next Week


#	Task	Owner	When
1	Confirm POC domain/subdomain (e.g., poc.valero.com or a test domain they own)	Jeremy	1 week before start
2	Export 2-3 representative Bind zone files in RFC 1035 format (run named-checkzone to validate)	Jeremy / Matt	1 week before start
3	Confirm SIEM type and Event Hub compatibility — Splunk? Sentinel? QRadar? The connector matters.	Valero Security team	1 week before start
4	Provision resource group rg-dns-poc inside the existing Enterprise Landing Zone	Valero Platform team	3 days before start
5	Create 2 Entra ID test accounts — 1 "Operator" (records only) + 1 "Admin" (full zone control) for RBAC testing	Jeremy	3 days before start
6	Share ACME / cert validation workflow details — which CA? which client (certbot, acme.sh, Venafi)? manual or scripted today?	Jeremy / Matt	1 week before start
7	Confirm geo-routing test endpoints — a US-facing IP and a UK-facing IP (if testing geo)	Jeremy	1 week before start

Charles required project documentations
3-4 week of April 2026 wrappup





